{
  "name": "pedia-urgences",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Tableau français d’urgence pédiatrique : 63 lignes importées, relecture et audit numérique non clinique.",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/calculator.js && node --check dist/protocols.js && node --check dist/catalog-data.js && node --check dist/catalog-audit.js && node --check dist/catalog-ui.js",
    "start": "python3 -m http.server 8080 --directory dist"
  }
}
