{
  "name": "pedia-urgences",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "PediDoses : calculs SMUR pédiatriques, fiches de préparation et questionnements de validation clinique.",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/calculator.js && node --check dist/protocols.js && node --check dist/catalog-data.js && node --check dist/catalog-audit.js && node --check dist/catalog-ui.js && node --check dist/patient-calculator.js && node --check dist/smur-data.js && node --check dist/smur-ui.js",
    "start": "python3 -m http.server 8080 --directory dist"
  }
}
